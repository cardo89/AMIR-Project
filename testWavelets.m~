clc
clear all
close all

[x, fs, Nbits] = wavread('robinhood.wav');

x = x(:,1);
X = cwtft(x(:,1),'scales',1:256,'wavelet','mexh');

xec = icwtft(X);

plot(x(:,1)), hold on, plot(Xrec,'r');

% Normilizing

range = max(a) - min(a);
a = (a - min(a)) / range;

range2 = y - x;
a = (a*range2) + x;